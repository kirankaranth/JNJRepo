common:
  pipelineType: app-dev
  debug: true

  versionCalculation:
    type: gitversion
    patternType: jnj-developer-branch
  unitTest:
    enabled: false

  
  lint:
    enabled: false
   
  
  resolveArtifacts:
    enabled: false
  staticAnalysis:
    enabled: false
    type: helm
    sonar:
      enabled: false
      type: sonar
      comment: "This project is for non-prod use and does not include any files to be analyzed by sonarqube"
    
   
  integrationTest:
    enabled: false

  regressionTest:
    enabled: false

  buildAnalysis:
    enabled: false

  deploy:
      enabled: false

